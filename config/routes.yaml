# yaml-language-server: $schema=../vendor/symfony/routing/Loader/schema/routing.schema.json

# This file is the entry point to configure the routes of your app.
# Methods with the #[Route] attribute are automatically imported.
# See also https://symfony.com/doc/current/routing.html

# To list all registered routes, run the following command:
#   bin/console debug:router

#requirements: определяет требования к параметрам маршрута с помощью: констант регулярных выражений, параметров конфигураций?
# если в маршруте есть переменная /blog/{...} ее он и будет проверять на соответствие. Требования могут быть встроены в параметр
# но это может нарушить читаемость если они длинные. /blog/{page<\d+>}

#condition: значение параметра может являтся выражением, в том числе может использовать синтаксис Symfony.
# помогает сделать логическую проверку на соответствие маршрута заданным параметрам.

#defaults: может задать сзначение по умолчанию для переменной /blog/{page}


controllers:
    resource: ../src/Controller/
    type:     attribute

